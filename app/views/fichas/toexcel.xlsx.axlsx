wb = xlsx_package.workbook
wb.add_worksheet(name: "Fichas") do |sheet|
    @fichas.each do |ficha|
        sheet.add_row [ficha.id, ficha.numero, ficha.especialidad.nombre ,
        ficha.naprendices, ficha.fechafin]
    end
end

