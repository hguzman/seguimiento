<!-- breadcrumb -->
<%breadcrumb :ambiente_anotacion_edit, @anotacion, @ambiente %>
<!-- informacion ambiente -->
  <%= render partial: 'cabecera' %>
<div class="row">
    <div class="w-100">
      <!-- anotacion -->
      <div class="row mt-3">
        <div class="col-12">
          <div class="ibox-content d-flex justify-content-between">
            <div class="col-md-12 d-flex flex-wrap">
              <div class="col-12 p-0">
                <h1>Actualizar anotación</h1>
                <!-- cuerpo de la anotación -->
                <div class="card border-secondar">
                  <div class="card-header">
                    <div class="col-12 d-flex justify-content-between p-0">
                      <div class="col-10 pl-0">
                        <h4 class="card-subtitle">Creada por:
                          <%= current_user.nombres%>
                            </strong>
                        </h4>
                      </div>
                      <div class="col-2 pr-0 text-right">
                        <button class="btn pr-0"><i class="fa fa-chevron-up"></i> Cargar archivo</button>
                      </div>
                    </div>
                  </div>
                  <div class="card-body">
                    <%= render partial: 'form' %>
                      <!-- Botones volver -->
                      <!-- <%= link_to "Volver", anotacion_comentarios_path(@anotacion), class: "btn btn-danger btn-block col-4 mt-3 mx-0"  %> -->
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>