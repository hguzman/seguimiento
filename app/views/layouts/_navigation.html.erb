<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <!-- <img alt="image" class="rounded-circle" src="image_path('profile_small.jpg') %>"/> -->
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="block m-t-xs font-bold"></span>
                        <span class="text-muted text-xs block"><%= current_user.nombre %> <%= current_user.apellido %>
                    </a>
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="block m-t-xs font-bold"></span>
                        <span class="text-muted text-xs block"><%= current_user.email %>
                    </a>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs">
                       <li><a class="dropdown-item" href=" <%= user_path %>" class="btn btn-nar">Mi Perfil</a></li>
                       <li><a class="dropdown-item" href="<%= change_password_user_path %>">Cambiar Contraseña</a></li>
                       <li><a class="dropdown-item" href="#">Buzón</a></li>
                       <li class="dropdown-divider"></li>
                       <li><%= link_to "Cerrar Sesion", destroy_user_session_path, method: :delete, class:"btn btn-nar"%></li>
                   </ul>
                </div>
                <div class="logo-element">
                    <%=link_to "SG20", root_path %>
                </div>
            </li>
            <li class="<%= 'active' if current_page?('/') %>">
                <%= link_to "/" do %>
                    <i class="fa fa-laptop"></i> <span class="nav-label">Inicio</span>
                    <% end %>
            </li>
            <li class="<%= is_active_controller('graphs') %>">
                <a href="#"><i class="fa fa-bar-chart-o"></i> <span class="nav-label" data-i18n="nav.graphs">Especialidades</span><span class="fa arrow"></span></a>
                <ul class="nav nav-second-level collapse <%= is_active_controller('graphs', 'in') %>">
                    <li class="<%= is_active_action('flot') %>">
                        <%= link_to "Listar", especialidades_path %>
                    </li>
                    <li class="<%= is_active_action('flot') %>">
                        <%= link_to "Crear", new_especialidad_path %>
                    </li>
                </ul>
            </li>
            <li class="<%= is_active_controller('graphs') %>">
                <a href="#"><i class="fa fa-bar-chart-o"></i> <span class="nav-label" data-i18n="nav.graphs">Fichas</span><span class="fa arrow"></span></a>
                <ul class="nav nav-second-level collapse <%= is_active_controller('graphs', 'in') %>">
                    <li class="<%= is_active_action('flot') %>">
                        <%= link_to "Listar", fichas_path %>
                    </li>
                    <li class="<%= is_active_action('flot') %>">
                        <%= link_to "Crear", new_ficha_path %>
                    </li>
                </ul>

            </li>
        </ul>

    </div>
</nav>
