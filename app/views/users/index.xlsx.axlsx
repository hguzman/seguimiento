wb = xlsx_package.workbook
wb.add_worksheet(name: "Users") do |sheet|
sheet.add_row ['id', 'nombres', 'apellidos', 'documeto']
@users.each do |user|
    sheet.add_row [user.id, user.nombres, user.apellidos, user.ndocumento]
  end
end